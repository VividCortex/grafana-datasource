/**
 * Based on the time interval and the number of expected points, calculate the sample size parameter.
 *
 * @param  {number} until
 * @param  {number} from
 * @param  {number} dataPoints
 * @return {number}
 */
export declare function calculateSampleSize(from: number, until: number, dataPoints: number): number;
